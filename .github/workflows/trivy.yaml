name: Trivy
on:
  pull_request:
  push:
    branches: [main, master]
jobs:
  terraform-trivy:
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      discussions: write
      pull-requests: write
      id-token: write
      security-events: write
    steps:
    - uses: dasmeta/reusable-actions-workflows/trivy@4.3.2
      with:
        fetch-depth: 0
    continue-on-error: true

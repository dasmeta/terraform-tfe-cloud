# Tasks

- [x] Define the MetaCloud-to-GitHub variable-set synchronization behavior.
- [x] Add the Terraform Cloud variable resource.
- [x] Document the bootstrap prerequisite.
- [ ] Import the existing variable into the repository manager state during
  rollout.
